# 执行记录：数据库迁移和前端API服务

> 执行时间：2025-01-15
> 状态：已完成

## 任务概述

创建数据库迁移脚本和前端API服务封装，完善开发和部署流程。

---

## 已创建的文件

### 1. 数据库迁移脚本（Flyway）

#### V4__create_notification_record_table.sql
**路径**: `/data/lingflow/backend/src/main/resources/db/migration/V4__create_notification_record_table.sql`

**功能**: 创建通知记录表

**表结构**:
```sql
CREATE TABLE lf_notification_record (
    id BIGSERIAL PRIMARY KEY,
    notification_id VARCHAR(64) UNIQUE NOT NULL,
    type VARCHAR(32) NOT NULL,
    recipient_id VARCHAR(64) NOT NULL,
    recipient_name VARCHAR(128),
    title VARCHAR(256) NOT NULL,
    content TEXT,
    process_instance_id VARCHAR(64),
    process_definition_id VARCHAR(64),
    process_definition_key VARCHAR(128),
    process_name VARCHAR(256),
    task_id VARCHAR(64),
    task_name VARCHAR(256),
    is_read BOOLEAN NOT NULL DEFAULT FALSE,
    read_time TIMESTAMP,
    create_time TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
    update_time TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
    extra_data TEXT
);
```

**索引**:
- `idx_notification_recipient` - 接收人ID索引
- `idx_notification_process_instance` - 流程实例ID索引
- `idx_notification_task` - 任务ID索引
- `idx_notification_type` - 通知类型索引
- `idx_notification_read` - 已读状态索引
- `idx_notification_create_time` - 创建时间索引

**注释**: 已添加表和字段注释，便于理解表结构

#### V5__create_process_comment_table.sql
**路径**: `/data/lingflow/backend/src/main/resources/db/migration/V5__create_process_comment_table.sql`

**功能**: 创建流程评论表

**表结构**:
```sql
CREATE TABLE lf_process_comment (
    id BIGSERIAL PRIMARY KEY,
    comment_id VARCHAR(64) UNIQUE NOT NULL,
    type VARCHAR(16) NOT NULL,
    process_instance_id VARCHAR(64),
    process_definition_id VARCHAR(64),
    task_id VARCHAR(64),
    task_name VARCHAR(256),
    user_id VARCHAR(64),
    user_name VARCHAR(128),
    content TEXT NOT NULL,
    parent_id VARCHAR(64),
    is_deleted BOOLEAN NOT NULL DEFAULT FALSE,
    create_time TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
    update_time TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
    delete_time TIMESTAMP,
    extra_data TEXT
);
```

**索引**:
- `idx_comment_process_instance` - 流程实例ID索引
- `idx_comment_task` - 任务ID索引
- `idx_comment_user` - 用户ID索引
- `idx_comment_type` - 评论类型索引
- `idx_comment_create_time` - 创建时间索引
- `idx_comment_parent` - 父评论ID索引
- `idx_comment_deleted` - 删除状态索引

**注释**: 已添加表和字段注释

### 2. 前端API服务

#### extended.ts
**路径**: `/data/lingflow/frontend/src/api/extended.ts`

**功能**: 封装所有扩展API接口，方便前端调用

**包含的API模块**:

1. **流程监控API**（6个接口）
   - `getProcessInstanceMonitor` - 获取流程实例监控信息
   - `getProcessDefinitionMonitor` - 获取流程定义监控信息
   - `detectTimeoutProcesses` - 检测超时的流程实例
   - `getAllRunningProcessMonitors` - 获取所有运行中的流程实例监控
   - `getActivityNodes` - 获取活动节点信息
   - `getProcessProgress` - 获取流程进度信息

2. **流程统计API**（8个接口）
   - `getProcessInstanceStatistics` - 获取流程实例统计
   - `getTaskStatistics` - 获取任务统计
   - `getProcessDefinitionStatistics` - 获取流程定义统计
   - `getDailyStatistics` - 获取每日流程统计
   - `getUserTaskStatistics` - 获取用户任务统计
   - `getAverageCompletionTime` - 获取流程平均完成时间
   - `getProcessTrend` - 获取流程趋势统计
   - `getTimeoutStatistics` - 获取超时流程统计

3. **流程通知API**（12个接口）
   - `sendTaskAssignedNotification` - 发送任务分配通知
   - `sendTaskCompletedNotification` - 发送任务完成通知
   - `sendProcessTimeoutNotification` - 发送流程超时通知
   - `sendProcessRejectedNotification` - 发送流程拒绝通知
   - `sendProcessApprovedNotification` - 发送流程通过通知
   - `sendProcessWithdrawnNotification` - 发送流程撤回通知
   - `sendTaskDelegatedNotification` - 发送任务转办通知
   - `sendCustomNotification` - 发送自定义通知
   - `getUserNotifications` - 获取用户的通知列表
   - `getUnreadNotificationCount` - 获取未读通知数量
   - `markNotificationAsRead` - 标记通知为已读
   - `markAllNotificationsAsRead` - 批量标记为已读

4. **流程评论API**（9个接口）
   - `addProcessComment` - 添加流程实例评论
   - `addTaskComment` - 添加任务评论
   - `addSystemComment` - 添加系统评论
   - `getProcessComments` - 获取流程实例的所有评论
   - `getTaskComments` - 获取任务的所有评论
   - `getAllCommentsForProcess` - 获取流程的所有评论（包括任务评论）
   - `deleteComment` - 删除评论
   - `updateComment` - 更新评论
   - `getCommentStatistics` - 获取评论统计

5. **流程变量API**（11个接口）
   - `getProcessVariables` - 获取流程实例的所有变量
   - `getProcessVariable` - 获取流程实例的单个变量
   - `setProcessVariables` - 设置流程实例变量
   - `updateProcessVariable` - 更新流程实例变量
   - `removeProcessVariable` - 删除流程实例变量
   - `getTaskVariables` - 获取任务的所有本地变量
   - `getTaskVariable` - 获取任务的单个本地变量
   - `setTaskVariables` - 设置任务本地变量
   - `updateTaskVariable` - 更新任务本地变量
   - `getHistoricVariables` - 获取历史变量
   - `getVariableHistory` - 获取变量的历史变更记录
   - `copyVariables` - 复制变量

**总计**: 46个API接口

**使用示例**:
```typescript
import {
  getProcessInstanceMonitor,
  getUserNotifications,
  addProcessComment
} from '@/api/extended'

// 获取流程监控信息
const monitor = await getProcessInstanceMonitor(processInstanceId)

// 获取用户通知
const notifications = await getUserNotifications(userId, 20)

// 添加流程评论
const commentId = await addProcessComment({
  processInstanceId: 'xxx',
  userId: 'user001',
  content: '请尽快处理'
})
```

---

## 技术亮点

### 1. Flyway数据库迁移
- **版本控制**: V4、V5版本号确保迁移顺序
- **自动执行**: Spring Boot启动时自动执行迁移
- **可回滚**: 支持回滚机制（需额外配置）
- **团队协作**: 迁移脚本纳入版本控制

### 2. PostgreSQL特性
- **BIGSERIAL**: 自增主键
- **TIMESTAMP**: 时间戳类型，自动设置默认值
- **BOOLEAN**: 布尔类型
- **TEXT**: 大文本类型
- **索引优化**: 为常用查询字段添加索引
- **注释**: 完善的表和字段注释

### 3. 前端API封装
- **TypeScript类型**: 完整的类型定义
- **统一调用**: 使用request实例统一处理
- **RESTful风格**: 遵循REST API规范
- **参数处理**: 支持路径参数、查询参数、请求体
- **模块化**: 按功能模块分组API

### 4. 数据库设计
- **UUID主键**: 使用UUID作为业务主键
- **索引策略**: 为常用查询字段添加索引
- **逻辑删除**: 使用is_deleted字段标记删除
- **时间戳**: create_time、update_time自动管理
- **扩展字段**: extra_data存储JSON扩展数据

---

## 使用指南

### 1. 执行数据库迁移

**方式一：自动执行**
```bash
# 启动Spring Boot应用，Flyway自动执行迁移
cd backend
mvn spring-boot:run
```

**方式二：手动执行**
```sql
-- 登录PostgreSQL
psql -U lingflow -d lingflow

-- 手动执行迁移脚本
\i backend/src/main/resources/db/migration/V4__create_notification_record_table.sql
\i backend/src/main/resources/db/migration/V5__create_process_comment_table.sql
```

**验证迁移**:
```sql
-- 查看Flyway迁移历史表
SELECT * FROM flyway_schema_history ORDER BY installed_rank;

-- 查看创建的表
\dt lf_*

-- 查看表结构
\d lf_notification_record
\d lf_process_comment
```

### 2. 前端调用API

**安装依赖**:
```bash
cd frontend
npm install
```

**在Vue组件中使用**:
```vue
<template>
  <div>
    <div v-for="notification in notifications" :key="notification.id">
      {{ notification.title }}
    </div>
  </div>
</template>

<script setup lang="ts">
import { ref, onMounted } from 'vue'
import { getUserNotifications, markNotificationAsRead } from '@/api/extended'

const notifications = ref([])

onMounted(async () => {
  // 获取用户通知
  const data = await getUserNotifications('user001', 20)
  notifications.value = data.data
})

const handleMarkAsRead = async (notificationId: string) => {
  await markNotificationAsRead('user001', notificationId)
  // 刷新列表
  const data = await getUserNotifications('user001', 20)
  notifications.value = data.data
}
</script>
```

**在React组件中使用**:
```tsx
import { useEffect, useState } from 'react'
import { getUserNotifications, markNotificationAsRead } from '@/api/extended'

function NotificationList() {
  const [notifications, setNotifications] = useState([])

  useEffect(() => {
    const fetchNotifications = async () => {
      const data = await getUserNotifications('user001', 20)
      setNotifications(data.data)
    }
    fetchNotifications()
  }, [])

  const handleMarkAsRead = async (notificationId: string) => {
    await markNotificationAsRead('user001', notificationId)
    // 刷新列表
    const data = await getUserNotifications('user001', 20)
    setNotifications(data.data)
  }

  return (
    <div>
      {notifications.map((notification: any) => (
        <div key={notification.id}>{notification.title}</div>
      ))}
    </div>
  )
}
```

### 3. API响应处理

**成功响应**:
```json
{
  "code": 200,
  "message": "success",
  "data": { /* 实际数据 */ }
}
```

**错误响应**:
```json
{
  "code": 500,
  "message": "错误信息",
  "data": null
}
```

**统一错误处理**:
```typescript
import request from './index'

request.interceptors.response.use(
  response => {
    const { code, message, data } = response.data
    if (code === 200) {
      return data
    } else {
      // 错误处理
      console.error(message)
      return Promise.reject(new Error(message))
    }
  },
  error => {
    // 网络错误处理
    console.error('Network Error:', error.message)
    return Promise.reject(error)
  }
)
```

---

## 文件清单

### 创建的文件

```
backend/src/main/resources/db/migration/
├── V4__create_notification_record_table.sql    # 通知记录表迁移脚本
└── V5__create_process_comment_table.sql       # 流程评论表迁移脚本

frontend/src/api/
└── extended.ts                                 # 扩展API服务封装
```

---

## 数据完整性

### 1. 外键关系
- `lf_notification_record.process_instance_id` → Flowable `ACT_RU_EXECUTION` 表
- `lf_notification_record.task_id` → Flowable `ACT_RU_TASK` 表
- `lf_process_comment.process_instance_id` → Flowable `ACT_RU_EXECUTION` 表
- `lf_process_comment.task_id` → Flowable `ACT_RU_TASK` 表

注意：实际外键约束未创建，避免影响Flowable引擎的正常运行

### 2. 数据一致性
- 通知记录与流程实例同步
- 评论记录与流程实例同步
- 删除流程时不删除通知和评论（保留历史）

### 3. 数据清理
- 定期清理过期通知（使用定时任务）
- 定期物理删除已删除的评论（使用定时任务）
- 可配置保留时间

---

## 性能优化

### 1. 索引优化
- 为常用查询字段添加索引
- 复合索引优化复杂查询
- 定期分析索引使用情况

### 2. 查询优化
- 使用分页查询避免大量数据加载
- 使用SELECT指定字段避免SELECT *
- 使用JOIN优化关联查询

### 3. 缓存策略
- 可使用Redis缓存热点数据
- 缓存用户未读通知数量
- 缓存评论统计信息

---

## 后续建议

1. **添加更多迁移脚本** - 为其他表创建迁移脚本
2. **添加数据回滚脚本** - 创建V4__rollback.sql、V5__rollback.sql
3. **前端状态管理** - 使用Pinia/Vuex管理全局状态
4. **前端组件库** - 基于API服务创建业务组件
5. **单元测试** - 为API服务编写测试用例
6. **集成测试** - 测试前后端集成
7. **继续开发** - 执行其他模块开发

---

## 总结

✅ **数据库迁移脚本已创建**
- V4: 通知记录表
- V5: 流程评论表
- 完整的索引和注释

✅ **前端API服务已封装**
- 46个API接口
- TypeScript类型支持
- 统一的调用方式

🎯 **开发和部署流程已完善**
- 数据库版本控制
- 前后端API对接
- 支持团队协作

📝 **后续建议**:
1. 执行数据库迁移
2. 前端集成新API
3. 创建业务组件
4. 编写测试用例
5. 继续执行其他模块开发

🎉 **数据库迁移和前端API服务已完成！**
